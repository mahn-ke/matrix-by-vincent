#matrix
version: '3.8'
services:
    synapse:
        image: matrixdotorg/synapse:v1.148.0
        container_name: synapse
        volumes:
            - data:/data
        ports:
            - "8008:8008"
        restart: unless-stopped
volumes:
    data:
